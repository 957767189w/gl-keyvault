{
  "name": "gl-keyvault",
  "version": "0.1.0",
  "description": "Secure API key management proxy for GenLayer Intelligent Contracts",
  "main": "dist/lib/index.js",
  "types": "dist/lib/index.d.ts",
  "bin": {
    "glvault": "./dist/cli/index.js"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc",
    "test": "jest --coverage",
    "lint": "eslint 'lib/**/*.ts' 'api/**/*.ts' 'cli/**/*.ts'",
    "cli": "ts-node cli/index.ts",
    "generate-key": "node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""
  },
  "dependencies": {
    "@vercel/kv": "^2.0.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@vercel/node": "^3.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0",
    "eslint": "^8.56.0",
    "@typescript-eslint/parser": "^6.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/genlayer-foundation/gl-keyvault"
  },
  "keywords": [
    "genlayer",
    "api-key",
    "proxy",
    "encryption",
    "intelligent-contracts",
    "blockchain",
    "security"
  ],
  "license": "MIT"
}
